% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_panel.R
\name{validate_panel}
\alias{validate_panel}
\title{Validate Data Quality of a Cleaned Panel Data Frame}
\usage{
validate_panel(
  data,
  time_col = NULL,
  na_warn_threshold = 0.5,
  sparse_warn_threshold = 0.1,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame}. The cleaned panel data to validate.}

\item{time_col}{A character string. Optional. Name of the time/date column to
check for temporal continuity. Default is \code{NULL} (skip time check).}

\item{na_warn_threshold}{Numeric between 0-1. Columns with NA rate above this
are flagged as high-risk. Default is \code{0.5}.}

\item{sparse_warn_threshold}{Numeric between 0-1. Columns with valid value rate
below this are flagged as sparse. Default is \code{0.1}.}

\item{verbose}{Logical. If \code{TRUE}, prints a human-readable report. Default is \code{TRUE}.}
}
\value{
A named list with quality metrics per column and summary flags.
}
\description{
\code{validate_panel()} performs a post-cleaning quality audit on a data frame.
It checks for NA rates, outliers (via IQR), duplicate rows, sparse columns,
columns that should be numeric but remain character, and optional time-series
gap detection. Returns a structured report and optionally prints a summary.
}
\examples{
# Toy example: validate a simple data frame
df <- data.frame(
  id   = c(1, 2, 2, 4),
  val  = c(100, 200, 200, NA),
  flag = c("1.5", "2.0", "2.0", "N/A")
)
report <- validate_panel(df, verbose = FALSE)
report$n_duplicates  # 1 duplicate row
report$high_na_cols  # columns with > 50\% NA

}
