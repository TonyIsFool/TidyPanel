% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_panel_structure.R
\name{detect_panel_structure}
\alias{detect_panel_structure}
\title{Diagnose the Structure of a Messy Excel Panel File}
\usage{
detect_panel_structure(path, sheet = 1, verbose = TRUE)
}
\arguments{
\item{path}{A character string. Path to the \code{.xlsx} or \code{.xls} file.}

\item{sheet}{A character string or integer. The sheet to analyse. Defaults to \code{1}.}

\item{verbose}{Logical. If \code{TRUE}, prints a human-readable summary to the console. Default is \code{TRUE}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{n_rows}}{Total rows in the raw sheet.}
\item{\code{n_cols}}{Total columns in the raw sheet.}
\item{\code{estimated_decoy_rows}}{Number of estimated metadata/noise rows at the top.}
\item{\code{multi_level_header}}{Logical. Whether multi-level (merged) headers are detected.}
\item{\code{has_temporal_cols}}{Logical. Whether wide-format temporal columns (years, quarters) are detected.}
\item{\code{n_aggregation_rows}}{Number of suspected Total/Sum aggregation rows found.}
\item{\code{n_phantom_cols}}{Number of fully-empty ghost columns.}
\item{\code{recommended_call}}{A character string with a suggested \code{read_messy_panel()} call.}
}
}
\description{
\code{detect_panel_structure()} performs a static, non-destructive analysis of a raw
Excel file and returns a structured report describing its "messiness". It detects
decoy metadata rows, multi-level headers, temporal wide columns, aggregation rows,
and phantom columns. It also generates a recommended \code{read_messy_panel()} call
based on the findings.
}
\examples{
# Toy example: detect structure from a temporary Excel file
tmp <- tempfile(fileext = ".xlsx")
df <- data.frame(
  Category = c("Total", "Revenue", "Cost"),
  `FY2022` = c("3M", "2M", "1M"),
  `FY2023` = c("4M", "2.5M", "1.5M"),
  check.names = FALSE
)
writexl::write_xlsx(df, tmp)
report <- detect_panel_structure(tmp, verbose = FALSE)
str(report)
unlink(tmp)

}
